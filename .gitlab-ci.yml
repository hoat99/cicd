services:
  - docker:19.03.7-dind
  - entrypoint: [ "dockerd-entrypoint.sh", "--tls=false" ]
stages:
  - build jar
  - build and push docker image
  - deploy
build:
  image: maven:3.6.3-jdk-11-slim
  stage: build jar
  tags:
    - local-docker
  before_script:
    - source .env
  script:
    - mvn install
    - mvn clean install  && mvn package
  artifacts:
    paths:
      - target/*.jar
docker build:
  image: docker:stable
  stage: build and push docker image
  tags:
    - local-docker
  before_script:
    - source .env
  script:
    - export DOCKER_HOST=tcp://127.0.0.1:2375
    - docker build -t $DOCKER_REPO .
    - docker login -u $DOCKER_USER -p $DOCKER_PASSWORD docker.io
    - docker push $DOCKER_REPO
deploy:
  image: ubuntu:latest
  stage: deploy
  tags:
    - local-docker
  script:
    - ssh users@172.24.160.1
    - docker login -u $DOCKER_USER -p $DOCKER_PASSWORD docker.io; docker stop $APP_NAME; docker system prune -a -f; docker pull $DOCKER_REPO; docker container run -d --name $APP_NAME -p $PORT $DOCKER_REPO; docker logout
